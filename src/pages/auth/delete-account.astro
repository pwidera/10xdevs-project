---
import Layout from "@/layouts/Layout.astro";
import { DeleteAccountConfirm } from "@/components/auth/DeleteAccountConfirm";

export const prerender = false;

const { user } = Astro.locals;
if (!user) {
  return Astro.redirect("/auth/login?next=" + encodeURIComponent("/auth/delete-account"));
}
---

<Layout title="Usuń konto">
  <main class="container mx-auto px-4 py-10">
    <DeleteAccountConfirm client:load />
  </main>
</Layout>
